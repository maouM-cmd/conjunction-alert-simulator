# Release v1.52.0 — Phase 10AS

**日付:** 2026-06-28

## 概要

purge async ポーリング UI、row_errors プレビュー CSV エクスポート、fleet summary 最終ページへジャンプ。

## Added

- purge async 後 `pollPurgeReloadHistoryTask` + task status purge message マップ
- retention dry-run プレビュー表から `preview CSV` ダウンロード（changes_only 対応）
- fleet summary 「最後へ」ボタン
