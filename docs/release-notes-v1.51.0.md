# Release v1.51.0 — Phase 10AR

**日付:** 2026-06-28

## 概要

dry-run row_errors プレビュー UI、reload purge async、fleet summary ページ番号。

## Added

- retention dry-run プレビュー表 errors 列 + row_errors 行
- `POST /prometheus/reload/history/purge?async_run=true` + Ops async ボタン
- fleet summary `ページ N/M` 表示
