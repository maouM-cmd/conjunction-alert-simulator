# Release v1.50.0 — Phase 10AQ

**日付:** 2026-06-28

## 概要

offset Enter キー、reload 履歴 purge 手動 API、dry-run JSON row_errors。

## Added

- fleet summary offset Enter でページ移動
- `POST /prometheus/reload/history/purge` + Ops purge ボタン
- dry-run JSON `row_errors[]` 構造化エラー
